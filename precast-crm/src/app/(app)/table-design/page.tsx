import { TableDesignerClient } from "./TableDesignerClient";

export const metadata = { title: "Table Image Designer" };

export default function TableDesignPage() {
  return <TableDesignerClient />;
}
