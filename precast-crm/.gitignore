node_modules
.next
.env
.env.local
*.log
.DS_Store
dist
build
coverage
.vercel
prisma/migrations/dev.db*

# User-uploaded files (delivery proofs, etc.) — local-only artifacts.
# Don't commit these to git; they live on the server's filesystem.
public/uploads/
